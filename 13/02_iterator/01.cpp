#include <vector>
#include <iostream>
using namespace std;

int main() {
    vector<char> a = {'h', 'j', 'k', 'l'};
    for (int i = 0; i < a.size(); i++) {
        cout << a[i] << endl;
    }
    return 0;
}
