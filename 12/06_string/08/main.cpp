#include <cstdio>
#include <cstdint>
#include <cstring>

int main() {
    char str[] = "Hello";
    printf("sizeof(str) = %ld\n", sizeof(str));
    printf("strlen(str) = %ld\n", strlen(str));
    return 0;
}
